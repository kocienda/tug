[package]
name = "tugedit-core"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Core library for the edit-program language (lexer, parser, resolver, applier, diff)"

[lib]
name = "tugedit_core"
path = "src/lib.rs"

[dependencies]
regex.workspace = true
similar.workspace = true
