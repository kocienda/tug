The python two-marker span cut — command lifted verbatim from `c992c30d-08b2-4808-89b9-f761014b0b7e.jsonl`; `tugapp/Sources/MainWindow.swift` is `tugapp/Sources/MainWindow.swift` at `f3a8b98c8`, with the private pasteboard type moved from `dev.tug.prompt-atoms` to `dev.tugapp.prompt-atoms` — the identity rename reached every file in the tree and this fixture with it, so the content is that commit's under today's names rather than byte-for-byte. Captured on macOS (BSD sed/awk).
