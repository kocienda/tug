sed -e 's/^  const lensCardIds = new Set<string>();$/  \/** Which sidebar card type each sidebar card id belongs to. *\/\n  const sidebarComponentByCardId = new Map<string, string>();/' tugdeck/src/layout-tree.ts > a.new && mv a.new tugdeck/src/layout-tree.ts && sed -e 's/^ \*   6. At most one pane hosts the Lens card, and that pane carries no$/ *   6. At most one pane hosts each sidebar card type, and that pane carries no/; s/^ \*      `slot` — the Lens is the imposition.s fixed end, not a link in$/ *      `slot` — a sidebar card is the imposition'"'"'s fixed end, not a link in/' tugdeck/src/layout-tree.ts > b.new && mv b.new tugdeck/src/layout-tree.ts