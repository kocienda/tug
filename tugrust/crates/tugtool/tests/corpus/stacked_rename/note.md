The stacked -e rename campaign in one call — command lifted verbatim from `3738fe1f-e179-46e8-abfb-33db19b4e600.jsonl`; `tugdeck/src/layout-tree.ts` is `tugdeck/src/layout-tree.ts` at `68e7d5a27`, with the app's reverse-DNS prefix swept from `dev.tugtool` to `dev.tugapp` — the identity rename reached every file in the tree and this fixture with it, so the content is that commit's under today's prefix rather than byte-for-byte. Captured on macOS (BSD sed/awk); the BSD-only `-i ''` spelling is rewritten to a `> tmp && mv`, which both seds read alike. The script is otherwise verbatim.
